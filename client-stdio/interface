input
data.spc
data.par

metadata in par
time start
time per scan
number of scans per spc

Functions to inplement
1. explode
explode a multiple scan EPR spec into multiple one scan spec
  readin any spc
  judge if it is multiple scan
  if single
  save as one dim
  if multiple
  save as multiple one dim
  save par as single dim
end
2. stackplot
  return a list to select from
  return a label list to label
  stackplot and save as png pdf
end
3. stack
  return a list to stack from
  stack multiple list
  return a para list to generate para from
  save par as single dim 
end
4. baselinecor
  call baseline correction
5. background cor
  call background scan
  subtract background spec from other spec
end

